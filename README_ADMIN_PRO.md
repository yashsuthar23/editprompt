# Admin Pro update
Extract the zip, then copy KEEPING THE FOLDERS:
- `admin-pro.js` and `server.js` go in the project ROOT (next to package.json)
- everything inside `public/` goes in the project's `public/` folder
Then run `npm start`. Rebuild EditPrompt.exe with `npm run build:exe` to include it.
